# Отчёт по заданию «стакан ликвидности»

Ветка: `codex/order-book-2026-09-16`.

Заполняется исполнителем по ходу работы, а не в конце. Приёмка читает этот
файл первым: она проверяет не то, что код написан, а то, что он делает
обещанное на живых данных.

---

## 1. Часть A. Что сделано

<!-- Какие модули появились, какие тронуты и зачем. Коротко, без пересказа
     диффа: дифф видно в git. -->

## 2. Часть A. Живая выдача

Правило проекта: расчётный модуль не считается проверенным, пока выдачу не
посмотрели глазами. Вставьте ответ `get_order_book` **как есть**, без правок,
минимум на трёх символах:

<!--
  1) ликвидный перпетуал (например BTCUSDT или ARBUSDT), limit=100
  2) тонкая монета — та, где сработает оговорка о покрытии и о пороге 2M
  3) тот же символ на market="spot" — два независимых ответа, тест A.9 №6
-->

```text
(сюда выдачу)
```

## 3. Часть B. Что сделано

<!-- Схема, допуск по весу, тикер, уборка, сводка. -->

## 4. Часть B. Живая сессия

Сессия на 5 минут с интервалом 10 секунд, запущенная вживую. Нужны три вещи:

<!--
  1) list_order_book_watches во время работы сессии
  2) get_order_book_watch_data в формате diff — видно ли события уровней
  3) он же в формате both — читается ли сводка B.7
-->

```text
(сюда выдачу)
```

## 5. Приёмочные тесты

| # | тест | файл и имя теста | статус |
|---|---|---|---|
| A.9 №1 | `limit` не из enum — ошибка валидации | | |
| A.9 №2 | неполное покрытие `depth_pct` печатается с причиной | | |
| A.9 №3 | `cum_notional_usdt` сходится с ручной суммой | | |
| A.9 №4 | оборот ниже 2M — `imbalance` = n/a с причиной | | |
| A.9 №5 | стены и кластеры = n/a «нет базы» | | |
| A.9 №6 | spot и futures — два независимых ответа | | |
| B.10 №1 | первый снимок в пределах `interval_sec` | | |
| B.10 №2 | превышение бюджета — отказ, снимков нет | | |
| B.10 №3 | достигнута `duration_min` — статус `expired` | | |
| B.10 №4 | истёк retention — данные удалены целиком | | |
| B.10 №5 | новый уровень между снимками — событие `appeared` | | |
| B.10 №6 | формат `diff` отдаёт готовую таблицу | | |
| B.10 №7 | допуск по остатку бюджета: одна принята, другая отклонена | | |

## 6. Что пришлось изменить против ТЗ и почему

<!-- Самый важный раздел. ТЗ писалось без доступа к коду; расхождения
     ожидаемы. Каждое: что в ТЗ, что сделано, чем обосновано (замер, правило
     проекта, невозможность). Молчаливое отступление хуже обоснованного. -->

## 7. Что осталось некалиброванным

<!-- Пороги, взятые из примеров ТЗ, и то, чем их можно будет померить, когда
     появятся наблюдения. -->

## 8. Что нужно от владельца при выкате

<!-- Переменные окружения, шаги, порядок. Compose и workflow вы не трогали —
     напишите здесь, что в них дописать. -->
